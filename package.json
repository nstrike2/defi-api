{
	"name": "defi-api",
	"version": "1.0.0",
	"main": "index.js",
	"repository": "git@github.com:nstrike2/defi-api.git",
	"author": "Neetish Sharma <neetishsharma2001@gmail.com>",
	"license": "MIT",
	"dependencies": {
		"concurrently": "^7.0.0",
		"cors": "^2.8.5",
		"ethers": "^5.6.1",
		"express": "^4.17.3",
		"nodemon": "^2.0.15"
	},
	"scripts": {
        "preinstall": "cd client && yarn install && cd .. && cd server && yarn install && cd .. && cd axelnet-blockchain && yarn install && cd ..",
        "client": "yarn --cwd client run start",
        "server": "yarn --cwd server nodemon index",
        "axelnet-blockchain": "yarn --cwd axelnet-blockchain run start",
        "start": "concurrently \"yarn run axelnet-blockchain\" \"yarn run server\" \"yarn run client\""
    }
}
